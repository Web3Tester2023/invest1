/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["tailwindui.com","images.pexels.com","cdn.dribbble.com"]
  }
}

module.exports = nextConfig
